---
spec: ./spec.md
status: draft   # draft → approved → in-progress → done
approved:       # 人間が承認した日付
progress: 0/0   # 完了数/総数
current:        # 現在実装中のタスク番号（例: 2.1）
---

# タスクリスト: <機能名>

<!--
状態表記のルール:
  - [ ] 1.1 未着手のタスク (spec §4.1)
  - [ ] 1.2 実装中のタスク (spec §4.2) ← 🔄実装中
  - [x] 1.3 完了したタスク (spec §4.3)

構成のルール:
- フェーズ0は環境変更（人間が同席してコマンドを1つずつ承認する枠）。
  spec §7 に書かれたものだけを載せる。不要なら「なし」と書く
- フェーズ1以降は自律実装。機能単位ごとに [RED]→[GREEN]→[REFACTOR] の順で並べる
- 各タスクに spec の該当セクション（§X.X。design.md のあるループでは必要に応じて design §D も併記）と「検証」方法を必ず書く
- 完了 = 検証が通り、チェックを付けた状態（コミットはフェーズ0完了時と全実装完了時の2回にまとめる）
-->

## フェーズ0: 環境セットアップ【人間承認枠】 (spec §7)

<!-- 環境変更が不要な場合は「なし」とだけ書く -->

- [ ] 0.1 `npm install <pkg>@<ver>` — <理由>
  - 検証: <バージョン確認方法など>

## フェーズ1: <機能単位A> (spec §4.1)

- [ ] 1.1 [RED] <機能A> のテスト作成
  - ケース: <正常系 / 異常系 / 分岐・境界値を列挙。例: 空入力・上限超え・権限なし>
  - 検証: テストを実行し、期待どおり失敗することを確認
- [ ] 1.2 [GREEN] <機能A> の実装
  - 検証: 1.1 のテスト + 既存テストが全部通る
- [ ] 1.3 [REFACTOR] <整理する内容>（不要なら作らない）
  - 検証: 全テストが通ったまま

## フェーズ2: <機能単位B> (spec §4.2)

- [ ] 2.1 [RED] <機能B> のテスト作成
  - ケース:
  - 検証: テストを実行し、期待どおり失敗することを確認
- [ ] 2.2 [GREEN] <機能B> の実装
  - 検証: 2.1 のテスト + 既存テストが全部通る

## 最終フェーズ: 仕上げ

- [ ] 9.1 全体テスト・Lint・ビルドの確認
  - 検証: <プロジェクトのテスト/Lint/ビルドコマンド>
